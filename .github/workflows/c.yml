name: C CI

on:
  push:
    branches: [ "master", "ci" ]
  pull_request:
    branches: [ "master", "ci" ]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - name: Install Make
      run: sudo apt-get install -y build-essential
    - name: Build
      run: |
        cd algebra-engine
        make